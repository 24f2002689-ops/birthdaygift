# Birthday Archive — scaffold

This workspace contains an initial scaffold for the interactive birthday website described in `birthday_website_master_spec.md`.

Run locally:

```bash
npm install
npm run dev
```

Next steps:
- Implement chapters and components per spec.

Push to GitHub (example):

```bash
git init
git add .
git commit -m "Initial scaffold for birthday site"
git branch -M main
git remote add origin git@github.com:USERNAME/birthday-site.git
git push -u origin main
```
